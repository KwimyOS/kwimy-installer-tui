# Development environment variables
# Env vars should be placed in the repo root
# - kwimy (root)
# -- kwimy-installer

###
### kwimy-installer
###

# Set to 1 to skip the network step in the installer
KWIMY_SKIP_NETWORK=0

# Set to 1 to force offline-only install behavior
KWIMY_OFFLINE_ONLY=0

# Force detected GPU vendors (comma separated), e.g. nvidia,intel,amd
KWIMY_DEV_GPU=

# Allow running installer without root (dev only)
KWIMY_DEV_ALLOW_NONROOT=0

# Adjust the terminal wrapper outer gap (used by live scripts)
KWIMY_OUTER_GAP=24

# Skip building the offline package repo during ISO builds
KWIMY_SKIP_OFFLINE_REPO=0

# Installer Base URL for pacman mirrors, e.g. https://mirror.kwimy.com/stable
KWIMY_PACMAN_MIRROR=https://mirror.kwimy.com/stable

# Installer Full mirrorlist contents (overrides KWIMY_PACMAN_MIRROR)
# Example:
# KWIMY_PACMAN_MIRRORLIST=Server = https://stable.mirror.kwimy.com/$repo/os/$arch\nServer = https://backup.mirror.kwimy.com/$repo/os/$arch
KWIMY_PACMAN_MIRRORLIST=

###
### kwimy-iso
###

# Resolve Offline Deps mirror url. e.g. https://mirror.kwimy.com/stable or https://mirror.kwimy.com/unstable
RESOLVE_MIRROR_URL=https://mirror.kwimy.com/stable

# Set `1` to skip the local build during ISO builds
KWIMY_SKIP_LOCAL_PKG_BUILD=1
